[project]
name = "rapport-glia"
version = "0.1.0"
description = "Rapport de stage chez Glia"
authors = [{ name = "Votre Nom" }]
dependencies = [
    "jupyter>=1.1.1",
    "pyyaml>=6.0.1",
    "ipykernel>=6.29.0",
]
requires-python = ">=3.14"

[tool.uv]
dev-dependencies = []
